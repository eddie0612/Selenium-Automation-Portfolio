# Selenium Automation Portfolio

這個專案展示了我使用 **C#** 與 **Selenium WebDriver** 建立的自動化測試與爬蟲架構。
內容包含針對「練習網站」的標準測試流程，以及針對「真實電商網站 (PChome)」的自動化查價實戰。

## 🚀 專案亮點 (Highlights)

### 1. PChome 24h 購物 - 自動化查價爬蟲 (`PChomeTests.cs`)
這是一個針對真實環境設計的資料蒐集工具，能夠自動搜尋清單中的商品並抓取價格。
* **動態輸入框處理**：模擬真實使用者鍵盤操作，解決 React 網頁輸入框無法直接清除 (Clear) 的限制。
* **智慧搜尋驗證**：自動比對搜尋結果與商品名稱，排除網站推薦系統產生的「不相關商品 (False Positive)」，確保資料準確性。
* **抗脆性設計 (Robustness)**：使用 `try-catch` 架構處理缺貨或未上市商品，確保單一任務失敗不會導致整個程式崩潰。
* **動態等待機制**：使用 `WebDriverWait` 配合 `CSS Selector` (data-regression)，精準處理網頁動態載入延遲。

### 2. SauceDemo - 標準電商測試流程 (`SauceDemoTests.cs`)
展示標準的自動化測試案例設計 (Test Automation Patterns)：
* **情境**：模擬使用者登入 -> 商品挑選 -> 加入購物車 -> 結帳驗證的完整流程。
* **架構**：使用 NUnit 的 `[SetUp]` 與 `[TearDown]` 進行瀏覽器生命週期管理。
* **驗證**：使用 `Assert.That` 進行嚴謹的結果比對。

## 🛠️ 技術棧 (Tech Stack)
* **Language**: C# (.NET 8.0)
* **Framework**: NUnit
* **Tool**: Selenium WebDriver (ChromeDriver)
* **Skills**: 
    * DOM Manipulation (XPath / CSS Selectors)
    * Explicit Waits (Synchronization)
    * Error Handling (Try-Catch)
    * Real-time Console Logging

---
*Created by Eddie. Welcome to review the code in `PChomeTests.cs` for real-world examples!*
